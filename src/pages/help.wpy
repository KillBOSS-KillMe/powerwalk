<style lang="less">
page{
  background-color: #efefef;
}
.titleImg{
  width: 750rpx;
  height: 380rpx;
  background-color: #fff;
}
.titleImg image{
  width: 750rpx;
  height: 360rpx;
}
.list{
  min-height: 700rpx;
  margin-top: 20rpx;
  margin-bottom: 110rpx;
}

.list, .con{
  width: 750rpx;
  height: auto;
  background-color: #fff;
}.con image{
  transform:rotate(90deg);
}

.listShow{
  width: 750rpx;
  height: 80rpx;
  line-height: 80rpx;
  color: #333;
  font-size: 30rpx;
  border-bottom: 1rpx solid #e5e5e5;
  display: flex;
  align-items: center;
  justify-content: center;
}
.listShow image{
  width: 20rpx;
  height: 30rpx;
  margin-left: 20rpx;
}
.listShow view{
  width: 650rpx;
}
.listShowSon{
  height: auto;
  padding: 20rpx 50rpx;
  color: #333;
  font-size: 25rpx;
}

.listHide .listShowSon{
  display: none;
}
.listHide image{
  transform:rotate(0deg) !important;
}
</style>

<template>
  <view>
    <view class="titleImg">
      <image src="/img/index_main.jpg" />
    </view>
    <view class="list">
      <view class="listCon">
        <!-- ===================列表中加入listHide  描述信息隐藏====================== -->
        <block wx:for="{{helpData}}" key="index" index="index" item="item">
          <view class="con {{selId === item.id?'':'listHide'}}" data-id="{{item.id}}" @tap="selShow">
            <view class="listShow">
              <view>{{item.name}}</view>
              <image src="/img/go.png" />
            </view>
            <view class="listShowSon">{{item.articleCon}}</view>
          </view>
        </block>
        <!-- <view class="con listHide" data-id="2" @tap="selShow">
          <view class="listShow">
            <view>余额用途余额用途</view>
            <image src="/img/go.png" />
          </view>
          <view class="listShowSon">asdfasd</view>
        </view>
        <view class="con listHide" data-id="3" @tap="selShow">
          <view class="listShow">
            <view>余额用途余额用途</view>
            <image src="/img/go.png" />
          </view>
          <view class="listShowSon">afdsasdf</view>
        </view> -->
      </view>
    </view>
    <nav @childFn.user="goPage" />
  </view>
</template>

<script>
import wepy from 'wepy'
import nav from '../components/nav' // 底部导航
export default class Help extends wepy.page {
  config = {
    'navigationBarTitleText': '帮助中心'
  };
  // 生命组件ID
  components = {
  // 底部导航
    nav: nav
  };
  // alias example
  data = {
    helpData:[
      {
        'id': 1,
        'name': 'topic',
        'articleCon': '答题答题答题答题答题答题答题答题答题答题答题答题答题答题答题'
      },
      {
        'id': 2,
        'name': 'challenge',
        'articleCon': '挑战挑战挑战挑战挑战挑战挑战挑战挑战挑战挑战挑战'
      },
      {
        'id': 3,
        'name': 'turntable',
        'articleCon': '转盘转盘转盘转盘转盘转盘转盘转盘转盘转盘转盘转盘转盘'
      }
    ],
    selId: null
  };
  methods = {
    // 底部导航跳转
    goPage (url, evt) {
      // 销毁当前页{跳转}
      this.$redirect(url)
    }
  };
  async onLoad(options) {};
  selShow(e) {
    this.selId = e.currentTarget.dataset.id
    this.$apply()
  }
}
</script>
